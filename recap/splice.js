// Array.prototype.splice()


const numbers = [1,2,3,4,5];

const result = numbers.splice(1,2,10,12,13,19);
console.log(numbers); // it change the existing array modify main array
console.log(result);// just remove item shows here